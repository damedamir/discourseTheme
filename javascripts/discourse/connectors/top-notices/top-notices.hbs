 {{#if this.isAdminPage}}
         <DButton
                @action={{action "navigateToDashboard"}}
                @icon="house"
                @label={{ theme-prefix "back_to_main_dashboard" }}
                class="ember-view admin-sidebar-nav-link"
              />
 {{/if}}